<h1 class="font-normal text-3xl mb-4">
  <%= @deck.name %>

  <%= link_to 'Add cards', new_deck_card_path(@deck), class: 'text-base' %>
</h1>

<%= link_to 'Browse', deck_cards_path(@deck), class: 'btn btn-outline-primary' %>
<%= link_to 'Study', '#', class: 'btn btn-primary' %>
